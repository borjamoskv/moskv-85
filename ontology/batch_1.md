# MOSKV-85 ONTOLOGY - BATCH 1
SYS_ID: borjamoskv
Reality Level: C5-REAL
Batch: 1/10

## MATRIZ 1: PRIMITIVAS DE COLAPSO (15/150)
| ID | Primitiva | Mecanismo Causal | Activación (Trigger) | Sensor (Síntoma) | Escala Temporal | Gravedad | Intervención |
|---|---|---|---|---|---|---|---|
| P001 | Stack Underflow | Pops de más valores que la longitud de la pila | Opcode de desapilado (`+`, `-`, `s`, `p`) en stack vacío | Excepción `IndexError` | Microsegundos | Alta | Retornar `0` por defecto en desapilados arbitrarios (Safe-Pop). |
| P002 | Buffer Lockup | Lectura bloqueante indefinida en un canal | Opcode `T` (recv) en canal vacío sin productores vivos | Thread bloqueado en `queue.Queue.get()` | Indefinido | Crítica | Definir timeout de canal o hilos daemon para evitar cuelgues del SO. |
| P003 | WAL Lockout | Concurrencia de múltiples hilos en SQLite sin WAL | Escritura paralela intensiva de opcodes `W` | Excepción `sqlite3.OperationalError: database is locked` | Milisegundos | Alta | Forzar `PRAGMA journal_mode=WAL;` y `busy_timeout=5000;` en el bootstrap. |
| P004 | Lexer Anergy Leak | Inyección de caracteres no válidos masivos | Procesar archivos con comentarios y espacios excesivos sin AOT | Degeneración en la velocidad de iteración en bucles | Segundos | Media | Filtro lexer en la compilación AOT para purgar masa estocástica. |
| P005 | String Escaping Trap | Cierre ausente de cadena literal | Uso de barra invertida única `\` al final del archivo | Consumo de tokens completo hasta EOF sin tokenizar opcodes finales | Microsegundos | Media | Manejo tolerante en el parser AOT retornando el string acumulado. |
| P006 | Division by Zero | División/Modulo por denominador nulo | Opcode `/` o `%` con un `0` en la cima de la pila | Excepción `ZeroDivisionError` en el kernel de Python | Microsegundos | Alta | Guardar la operación con un retorno condicionado a `0` si el denominador es 0. |
| P007 | Unbalanced Brackets | Bloques `[` sin emparejarse con `]` | Código mal formateado evaluado directamente | Excepción `SyntaxError` durante `compile_aot` | Microsegundos | Alta | Validar la paridad de corchetes mediante contador de profundidad antes de compilar. |
| P008 | Orphan Shared State | Modificación asíncrona de canales o memoria | Spawn de VM (`Y`) sin heredar punteros mutables correctos | Pérdida de comunicación de canal o excepciones `KeyError` | Milisegundos | Alta | Estructurar un diccionario `shared_state` compartido por referencia exacta. |
| P009 | Thread Exhaustion | Spawn indiscriminado de hilos | Bucle infinito conteniendo la instrucción `Y` | Gotera de descriptores de hilos del sistema operativo | Segundos | Crítica | Limitar el pool de hilos o imponer cuotas de spawning en la VM. |
| P010 | Type Collision | Evaluación de tipos no soportados | Pasar un escalar a un opcode de control de flujo (`f`) que espera un AST | Excepción `TypeError` en tiempo de ejecución | Microsegundos | Alta | Validación estricta con `isinstance` antes del despacho en `step`. |
| P011 | Out-of-Alphabet Bypass | Intento de inyección de opcodes ajenos al set de 85 | Caracteres como `w`, `x`, `y`, `z` usados como opcodes directos | Silencioso descarte de la instrucción en `compile_aot` | Microsegundos | Baja | Exigir re-mapeo del set de opcodes a símbolos válidos del alfabeto Base-85. |
| P012 | Memory Leakage | Ingesta ilimitada en la memoria mapeada | Bucle infinito de almacenamiento `g` con claves incrementales | Desbordamiento de la memoria heap del host | Horas | Media | Establecer límites virtuales sobre la cardinalidad de la tabla `memory`. |
| P013 | Integer Overflow | Cálculo matemático exponencial masivo | Uso recurrente de `^` sobre bases grandes | Ralentización extrema de Python por manejo de enteros grandes | Milisegundos | Media | Imponer límites de tamaño (ej. truncado a 64-bit firmado) en operaciones aritméticas. |
| P014 | Standard Prelude Collision | Sobreescritura accidental de opcodes nativos | Reasignación de letras reservadas | Pérdida de comportamiento estándar | Microsegundos | Media | Proteger la inmutabilidad de la tabla de despacho nativa del kernel. |
| P015 | Thread Deadlock | Dos hilos esperando recíprocamente por canales cruzados | Consumidores de canales cruzados sin productores activos concurrentes | Suspensión indefinida de la ejecución de la VM | Indefinido | Crítica | Implementar timeouts asíncronos en canales para abortar hilos huérfanos. |

## MATRIZ 2: INVARIANTES TERMODINÁMICAS (15/150)
| ID | Invariante | Lógica / Principio | Implicación Operacional | Condición de Borde | Métrica Falsable |
|---|---|---|---|---|---|
| I001 | Conservación del Alfabeto | El conjunto de entrada $C$ debe pertenecer estrictamente a $ALPHABET$ ($33 \le ASCII \le 117$) | Cualquier carácter fuera de rango es anergía y se purga estáticamente. | Exclusión de delimitadores especiales (`\`, `[`, `]`) de la regla de descarte en el lexer. | $\forall c \in AST, c \in ALPHABET\_SET \lor isinstance(c, (int, list, str))$ |
| I002 | Isomorfismo del AST | El AST optimizado debe comportarse funcionalmente idéntico al AST crudo sin optimizar. | La optimización de constantes no debe alterar las ramas de control condicional dinámico. | Evitar colapsar variables que dependan de entradas externas (`i`, `n`, `Z`). | `vm.execute_ast(ast) == vm.execute_ast(optimized_ast)` |
| I003 | Preservación del Estado en Spawn | Cada sub-intérprete en un hilo secundario posee su propia pila aislada de datos. | Un hilo no puede corromper o desapilar directamente la pila de otro hilo activo. | Compartición controlada de `memory` y `channels` a través de referencias compartidas. | `len(hilo_principal.stack)` no varía ante ejecuciones del `hilo_secundario.stack`. |
| I004 | Límite de Landauer (Purga de Anergía) | La destrucción de información (comentarios, espacios) genera calor pero libera el ancho de banda del bucle. | Toda anergía léxica debe ser purgada AOT para reducir la complejidad de ejecución a $O(1)$. | Mantener los saltos de línea estrictamente dentro de los string literals delimitados (`\`). | `len(compile_aot(code)) < len(code)` si `code` contiene espacios o comentarios. |
| I005 | Consenso Quorum Bizantino | La alteración física de disco requiere la aserción de Quorum $N \ge 3$. | Ninguna escritura física por el opcode `W` es autorizada sin confirmación de firmas independientes. | Fallo total del consenso si la base de datos de propuestas SQLite WAL no es accesible. | `proposals.votes >= 3` para autorizar la mutación en el sistema de archivos del host. |
| I006 | Inmutabilidad de Opcodes | Los 85 opcodes del alfabeto Base-85 tienen despacho estático y rígido en tiempo de ejecución. | El intérprete no puede alterar dinámicamente el comportamiento interno de un carácter. | Reflexión limitada por ejecución de enteros en stack mediante el opcode `e`. | `step(inst)` despacha deterministicamente la misma función para la misma `inst`. |
| I007 | Determinismo Matemático | Operaciones idénticas sobre el stack deben retornar idénticos resultados de pila. | La VM no introduce varianza lógica excepto ante llamadas explícitas del hardware RNG (`R`). | La semilla del generador pseudoaleatorio no debe interferir con la consistencia del AST. | `f(a, b)` con entradas estáticas produce salidas estáticas invariantes. |
| I008 | Aislamiento de Sandbox (Seatbelt) | La ejecución de la VM está acotada al árbol de directorios del proyecto y `/tmp`. | El kernel rechaza interactuar con rutas del sistema protegidas en macOS (Directiva R5). | Elevación de privilegios bloqueada por el perfil del SO del host. | Excepciones controladas al intentar escribir en directorios restringidos. |
| I009 | Coherencia del Stack | La cantidad de elementos extraídos y empujados debe equilibrar la masa de la pila. | Cada instrucción tiene una firma de pila fija (ej. `+` consume 2, empuja 1). | Las operaciones de manipulación especial (`c`, `l`, `U`) alteran la firma. | `len(stack_final) == len(stack_inicial) - net_diff`. |
| I010 | Sincronización de WAL | Toda escritura en la DB transaccional debe usar WAL y exclusión de cerrojo. | Evita el estado corrupto de datos ante concurrencia de hilos de la VM. | Timeout de base de datos fijado estrictamente en 5000ms. | Ausencia de excepciones `database is locked` en pruebas de estrés concurrentes. |
| I011 | Consistencia del Tiempo | Los hilos de la VM no pueden interferir con el reloj del host. | El scheduler de sub-procesos de Python gestiona de forma transparente el tiempo de CPU. | Sobrecarga de hilos que cause congelamiento del ciclo de reloj interno de la VM. | La ejecución de `stress_test` finaliza dentro del timeout límite de 5s. |
| I012 | Preservación del Creator Magic | El opcode `k` debe empujar siempre la constante inalterable `858585`. | Asegura la procedencia de la VM y la autoría de Borja Moskv (SYS_ID). | Modificación de código fuente para falsificar la constante. | `vm.execute_block("k"); vm.pop() == 858585`. |
| I013 | Safe-Pop por Defecto | Un stack vacío siempre devuelve `0` ante un intento de pop. | Evita fallos críticos de desbordamiento en flujos interactivos dinámicos. | Exceso de desapilado deliberado en algoritmos recursivos. | `vm.stack.clear(); vm.pop() == 0`. |
| I014 | Delimitación AOT | Los delimitadores de bloques y strings no se consideran opcodes de ejecución. | El Lexer AOT debe consumirlos y usarlos para estructurar el AST, no para ejecutar. | Presencia de delimitadores sin escape dentro de strings literales. | `[`, `]`, `\` no aparecen como opcodes planos ejecutables en el AST. |
| I015 | Finalización de Daemon | Los hilos secundarios mueren incondicionalmente al salir el hilo principal. | No se permiten hilos huérfanos que mantengan vivo el proceso de la VM tras un halt (`u`). | Cuelgues en la lectura de entrada estándar (`i`, `n`) dentro de sub-procesos. | `interpreter.running = False` o `sys.exit()` destruye el proceso de inmediato. |

## MATRIZ 3: ANTIPATRONES ESTOCÁSTICOS (10/40)
| ID | Antipatrón | Disfunción Causal | Señal de Presencia | Impacto en Robustez | Refactor (Alternativa) |
|---|---|---|---|---|---|
| A001 | Hot Parsing loops | Escaneo repetido de strings dentro de bucles `while` (`f`) | Parsing redundante lento en runtime | Pérdida de throughput O(N) | Compilador AOT que genera y cachea el AST en memoria. |
| A002 | Stack Overflow DUP3 | Duplicación incontrolada de elementos por mala lógica en el opcode `:` | Crecimiento incontrolado de la pila | Agotamiento de la memoria RAM del host | Corrección de la lógica de duplicación recursiva de la triada en la pila. |
| A003 | Silent Fails on Write | Retorno de código de éxito en `W` sin validar Quorum BFT | Mutaciones de disco sin aserción real | Incoherencia de estado entre hilos | Implementar retorno explícito de estado `0` ante fallo de Quorum en base de datos WAL. |
| A004 | Hardcoded Pathing | Uso de directorios absolutos del host para persistencia de base de datos | Fallo de ejecución en entornos ajenos | Pérdida de portabilidad del kernel | Inyección dinámica de `db_path` y uso de rutas de sandbox en `/tmp`. |
| A005 | Non-Daemon Threads | Spawning de hilos con `daemon=False` por defecto | Procesos zombi vivos tras finalizar el hilo principal | Bloqueo persistente de la terminal del IDE | Forzar `t.daemon = True` antes de inicializar el hilo. |
| A006 | Out-of-Alphabet Opcodes | Intentar añadir nuevas funciones de VM usando caracteres no Base-85 | Opcodes que se ignoran silenciosamente | Confusión y bugs en lógica de usuario | Re-mapeo estricto del set de instrucciones utilizando caracteres válidos de `ALPHABET`. |
| A007 | Unshielded Division | División directa sin validar divisor en la VM | Crash del proceso principal por excepciones aritméticas | Inestabilidad en producción | Capturar divisor y devolver `0` si es nulo. |
| A008 | Nested Concurrency Creation | Hilos secundarios creando canales dinámicos sin lock | Colisiones en la tabla global de canales | Race conditions y duplicación de IDs | Delegar la creación y el contador en un `shared_state` thread-safe. |
| A009 | Non-WAL Database | Uso de SQLite en modo por defecto (Rollback Journal) | Bloqueo concurrente de escrituras | Excepciones continuas de base de datos | Forzar el modo `WAL` y timeout robusto en el bootstrap de la VM. |
| A010 | Literal Hot Decoding | Decodificación Base-85 de `#` y `$` en tiempo de ejecución | Overhead matemático redundante en bucles | Retraso en el ciclo de ejecución | Decodificar y cachear los valores enteros directamente en la matriz AST en AOT. |

## MATRIZ 4: REDUNDANCIAS ACTIVAS (5/20)
| ID | Redundancia C5 | Función Topológica | Riesgo Mitigado | Coste (Overhead) | Dependencias |
|---|---|---|---|---|---|
| R001 | BFT Validator Quorum | Triple auditoría independiente (N=3) sobre propuestas de escritura | Escritura de datos corruptos o falsificación de transacciones | Latencia de spawning de hilos (milisegundos) | `sqlite3`, `threading` |
| R002 | Shared State Mirroring | Puntero compartido único de memoria y canales | Desincronización de recursos en la red de hilos | Locks de contención en lectura/escritura | `threading.Lock` |
| R003 | Dual AST Cache | Almacenamiento previo de bloques compilados en el despachador | Re-compilación redundante de bloques dinámicos | Consumo marginal de memoria RAM | Memoria interna de la VM |
| R004 | Double Stack Length Opcodes | Existencia de opcodes redundantes `l` y `U` para longitud de pila | Fallo sintáctico en scripts por uso de diferente alias | Duplicación menor en la lógica de `step` | Tabla de despacho de opcodes |
| R005 | SQLite Busy Timeout | Configuración de timeout de 5000ms en el driver transaccional | Colisión y muerte por deadlocks en escrituras concurrentes | Pequeño delay ante contención masiva de disco | Sistema operativo del host |

## MATRIZ 5: VECTORES DE ATAQUE ADVERSARIAL (10/50)
| ID | Vector Adversarial | Superficie de Ataque | Mecanismo de Explotación | Impacto Termodinámico | Defensa (Mitigación) |
|---|---|---|---|---|---|
| V001 | AST Depth Exhaustion | Compilador AOT (Recursión) | Enviar un archivo con miles de corchetes `[` anidados sin código | Desbordamiento de la pila de llamadas de Python (RecursionError) | Limitar la profundidad de recursión en el parser AOT. |
| V002 | Database Lock Attack | Base de datos de consenso WAL | Bloquear el archivo `.db` desde el host para causar busy lockups | Detención completa de las operaciones de escritura `W` en la VM | busy_timeout de 5000ms y control de excepciones. |
| V003 | Channel Flood Attack | Cola de canales concurrentes | Spawnear hilos enviando trillones de mensajes al canal sin descolar | Consumo y agotamiento de la memoria heap del proceso | Limitar el tamaño de buffer de las colas de canales. |
| V004 | Arbitrary Code Execution (e) | Opcode `e` (Eval) | Inyección de strings dinámicos formados desde el exterior | Ejecución de instrucciones destructivas en la VM | Sanitizar la entrada de strings antes de evaluar con `compile_aot`. |
| V005 | Disk Exhaustion Attack | Opcode `W` (Write) | Bucle infinito de escrituras concurrentes de payloads gigantes a disco | Llenado del disco físico del host | Imponer cuotas de escritura por sesión en la VM. |
| V006 | Path Traversal | Opcodes `W` y `Z` | Intentar leer/escribir usando rutas de escape relativas (ej. `../../`) | Acceso o corrupción de archivos fuera de la sandbox del proyecto | Forzar normalización de rutas y validación estricta de sub-directorios. |
| V007 | CPU Starvation (f) | Opcode `f` (While) | Evaluar una condición estática que siempre sea distinta de `0` sin cuerpo | Consumo de CPU al 100% en un bucle infinito | Introducir límites de iteración máxima en el runtime o timeouts. |
| V008 | Host RNG Contamination | Opcode `R` (Random) | Corromper el generador de entropía del sistema operativo | Salidas pseudoaleatorias predecibles | Uso de fuentes criptográficamente seguras (`secrets.SystemRandom`). |
| V009 | String Memory Exhaustion | Parser de cadenas literales `\` | Enviar una cadena de texto sin escape final que consuma gigabytes de EOF | OOM (Out Of Memory) en el parser de Python | Limitar el búfer de caracteres leídos por literal string. |
| V010 | Shared Memory Collision | Opcode `g` (Store) | Hilos concurrentes sobreescribiendo las mismas direcciones de RAM | Incoherencia de estado de memoria en hilos paralelos | Introducir bloqueos MUTEX en opcodes de memoria (`g` y `h`). |
